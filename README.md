# AudioSpeedHack

> [Article](https://absx.pages.dev/articles/speedup.html) about this repo

**WIP**

## Develop

- run `cargo b` to build the lib
- run `python test.py& && cargo run` to test dll inject
